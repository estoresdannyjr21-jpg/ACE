# Base URL for API (no trailing slash). With Vite dev proxy use /api so requests go to backend.
VITE_API_URL=/api

# Optional: JWT for development. Or set via sessionStorage in browser console: sessionStorage.setItem('auth_token', 'YOUR_JWT')
# VITE_AUTH_TOKEN=eyJhbGc...
